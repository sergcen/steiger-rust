# steiger-rust

Native npm distribution of the Steiger Feature-Sliced Design architecture linter.

```bash
npx steiger-rust ./src
```

The package installs a platform-specific Rust binary through an optional dependency. Do not install it with `--omit=optional`.
